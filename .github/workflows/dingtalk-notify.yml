name: DingTalk Issue Notification

on:
  issues:
    types: [ opened ]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send DingTalk Notification
        uses: visiky/dingtalk-release-notify@main
        with:
          DING_TALK_TOKEN: ${{ secrets.DINGTALK_ACCESS_TOKEN }}
          notify_title: '🔥 @${{ github.event.issue.user.login }} 创建了 issue：${{ github.event.issue.title }}'
          notify_body: '### 🔥 @${{ github.event.issue.user.login }} 创建了 issue：[${{ github.event.issue.title }}](${{ github.event.issue.html_url }}) <hr /> ![立即前往处理此 Issue](${{ github.event.issue.html_url }})'
          notify_footer: '> 💬 欢迎前往 GitHub 进行讨论，社区可能需要你的帮助。'
          at_all: false # whether to ding everybody
